package com.cydonic.nvs

import android.app.Application

class NanoVSApplication : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize dependencies, logging, etc.
    }
}